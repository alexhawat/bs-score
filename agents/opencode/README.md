# bs_score — OpenCode

Wrapper for **OpenCode**. Canonical skill + scorer: repository root of [alexhawat/bs_score](https://github.com/alexhawat/bs_score).

Install: register this skill folder with OpenCode, or load the repo-root `SKILL.md`. Set `BS_SCORE_ROOT` to the clone for `score.py`.
