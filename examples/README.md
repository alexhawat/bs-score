# Examples

```bash
python3 ../score.py findings.valid.json
python3 ../score.py findings.review.json
python3 ../score.py findings.skill.json
python3 ../score.py findings.agent.json
```

Repo/PR audits: two passes (claims + deep code). Review audits: one finding per failure mode.
