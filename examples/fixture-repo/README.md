# demo

A deliberately dishonest example project. Nothing here runs; the files exist so
`examples/findings.valid.json` has real text to quote, and so the verification
pass has something to verify.

## Features

- Supports OAuth2 login out of the box
- Real-time streaming endpoint at /v1/stream
- API key authentication

## Install

Run `demo install --fast` to set everything up.
